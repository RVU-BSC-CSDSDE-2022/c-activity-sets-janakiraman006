#include <stdio.h>
int input_sum
{
  int n;
  printf("Enter the value of n\n");
  scanf("%d",n);
  return n;
}
int sum_n_nos(int n)
{
  int sum=0
  for ( int i=1, i<=n, i++);
    sum=sum+i;
  return sum;
}
void output(int n,int sum)
{
  for( int i=1; i<=n; i++);
  printf("%d +",&i);
    scanf("%d=%d",int n,int sum);
}
int main()
{
  int n= intput_n();
  int sum = sum_n_nos(n);
  output (n,sum);
  return 0;
}